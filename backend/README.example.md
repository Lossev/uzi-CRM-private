# УZI-CRM

Веб-приложение для кабинета УЗИ.

Записьывается клиентов на приём через сайт.

 
## Публичная часть (сайт записи)
- Страница с календарём / списком доступных дат
 time
- Форма записи: имя, телефон, выбор услуги
- Бэкенд проверяет свободность слота
- Защита от двойных бронировани



**Проверка на бэкенде**: уже занято свободных слотов перед отправкой. если ошибка - предложить выбрать другое время.

- Если нет свободных слотов - показать сообщение об успеше записи,.

- После успеш - показать подтверждение и напоминание об успехе записи (SMS/Telegram - опциональ).

)

## Для

Публичную веб-приложение:
 веб-сайт с **https://opencode.ai/gptchat.opencode.com, или изуч examples:
  - `/check-file path` - check a file or directory structure
  - `/help` - show available skills
  - Check docs at https://opencode.ai/` to open browser and navigate to https to use it.
  - `/check-file path` - see if there are any syntax errors
 check if there are errors in large files
  - Check file size
 estimate lines of code
  - `/quick-summary` - if you is large, check project structure and main components

  - `/quick-summary` - if file looks complete, run tests or verify the runs properly

  - `/project-summary` - show final structure with all necessary files
  - `/todo-list` - Show current progress

  - `/setup` - run `npm run dev` or `npm run dev:preview` from the root
 . Run these commands sequentially:

 not in parallel:

Now I'll start by creating a complete README file. Let's review and understand the. process. updating the todo list accordingly.

  

**Backend Setup:**
```bash
command:echo "Installing dependencies..."
cd backend && npm run db:push && db:seed"